// ─────────────────────────────────────────────────────────────────────────────
// Патч для ManagedApplicationModule.bsl расширения ВыгрузкаВсехВнешнихОбработок
// ─────────────────────────────────────────────────────────────────────────────
//
// Куда добавить:
//   Конфигуратор → Расширения → ВыгрузкаВсехВнешнихОбработок →
//   Модуль управляемого приложения → найти процедуру Расш2_ПриНачалеРаботыСистемы
//   и добавить блок ниже рядом с BatchExportAddExt / BatchGetExtensionsList.
//
// Как вызывает Rust (processings.rs::discover_storage_via_extension):
//   1cv8.exe ENTERPRISE /Sserver\db /Nlogin /Ppassword
//     /CBatchGetProcessingsStructure;Out=C:\temp\...\processings-discovery-....json
//     /OutC:\temp\...\processings-discovery-....log
//
// Файл JSON пишется на КЛИЕНТСКОЙ машине (там где запущен 1c-export.exe),
// поэтому запись делается в клиентском контексте; серверная процедура
// только ВОЗВРАЩАЕТ структуру (см. ПолучитьСтруктуруХраненияСправочникаДопОбработок.bsl.txt).
//
// ─────────────────────────────────────────────────────────────────────────────

// Вставить внутрь процедуры Расш2_ПриНачалеРаботыСистемы:

Если СтрНайти(КоманднаяСтрока, "BatchGetProcessingsStructure") <> 0 Тогда

	ПутьКJSON = ПолучитьПараметрИзC(КоманднаяСтрока, "Out");

	Если ПустаяСтрока(ПутьКJSON) Тогда
		ВызватьИсключение "Для BatchGetProcessingsStructure необходимо указать параметр Out=ПутьКJSON";
	КонецЕсли;

	// Получаем Структуру с сервера — запись на клиенте
	// (сервер 1С может быть на другой машине и не иметь доступа к пути клиента).
	Результат = Расш2_ВыгрузкаДопОбработокИОтчетовСервер.ПолучитьСтруктуруХраненияСправочникаДопОбработок();

	ЗаписьJSON = Новый ЗаписьJSON;
	ПараметрыЗаписи = Новый ПараметрыЗаписиJSON(ПереносСтрокJSON.Авто, "  ");
	ЗаписьJSON.ОткрытьФайл(ПутьКJSON, , , ПараметрыЗаписи);
	ЗаписатьJSON(ЗаписьJSON, Результат);
	ЗаписьJSON.Закрыть();

	ЗавершитьРаботуСистемы(Ложь);

КонецЕсли;
